/// Domain module: Global Setting, Workspace Setting, Effective Setting,
/// Workspace containers, Agent Worktree lifecycle, focused session (Main Repo / Agent),
/// Workspace Secret Store, Overlay (Editor / Background CLI) lifecycle, and Command Mode (Leader).
enum DomainModule {}

# Native macOS first

Symphonia ships as a native macOS app that embeds libghostty. Symphonia owns Worktree lifecycle, Workspaces, Secret Store, Overlays, and Command Center; libghostty owns terminal fidelity.

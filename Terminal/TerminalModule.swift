/// Terminal module entry — AppKit / GhosttyKit island lives here.
/// See `TerminalSurfaceView`, `GhosttyChromeTheme`, `scripts/build-ghosttykit.sh`, and `libghostty-plan.md`.
enum TerminalModule {}
